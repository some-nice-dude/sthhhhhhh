# Required
OPENAI_API_KEY=sk-REPLACE
SHARED_SECRET=REPLACE_WITH_STRONG_RANDOM

# Model
MODEL_NAME=gpt-4o-mini

# Pacing and timeouts
RPM=2
TIMEOUT_SECS=18
REQ_TIMEOUT_SECS=45
MIN_DELAY_SECS=0

# Queue
QUEUE_SIZE=128

# Server binding (Render injects PORT)
HOST=0.0.0.0
PORT=8000
